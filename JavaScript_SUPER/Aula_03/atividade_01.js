// Atividade 01
// Algoritmo contagen regressiva com Loop

// Variaves e Entrada de Dados

let contador = parseInt(prompt("Informe um número"));

// Processando loop
while (contador > -1) {
    // Saida de dados
    console.log(contador);
    contador--;
}